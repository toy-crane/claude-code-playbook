---
title: '약한 신호도 누적되면 패턴이 된다 | Compound'
description: '한 사이클의 강한 신호는 즉시, 약한 신호는 누적된 뒤에 Skill·Hook·Rule·CLAUDE.md로 승격됩니다'
public: true
---

import { ImmediateVsAccumulatedPromotion } from '@/components/diagrams/immediate-vs-accumulated-promotion';

## Overview

배포까지 끝났습니다. 한 feature 사이클이 닫혔고, `artifacts/feedme/learnings.md`에 그 사이클의 판단·실패·우회 흔적이 기록되었습니다.

이번 레슨은 SDD 6단계의 마지막 -- **Compound** -- 입니다. 한 사이클의 학습이 다음 사이클의 도구가 되는 메커니즘을 봅니다. 본 챕터에서는 한 사이클만 돌렸으므로 도구 갱신을 직접 실행하기보다, **개념과 시뮬레이션으로** 다룹니다.

### 학습 목표

- 즉시 승격과 누적 회고의 차이를 구별합니다
- 약한 신호의 정의와 수집 대상을 설명합니다
- 4가지 승격 메커니즘이 어떤 반복에 적합한지 판단합니다

## 두 종류의 학습 — 즉시 승격과 누적 회고

learnings.md에 들어가는 학습은 신호의 강도에 따라 두 갈래로 갈립니다.

<ImmediateVsAccumulatedPromotion />

**즉시 승격은 강한 인사이트 1개로 충분**합니다. 같은 에러를 두 번 만났다면 다음에도 일어날 가능성이 매우 높으므로, 그 자리에서 Rule이나 Hook으로 만듭니다. /execute-plan Step 6에서 자동으로 처리됩니다.

**누적 회고는 약한 신호가 패턴으로 굳을 때까지 기다립니다.** 한 번만 보인 어색함은 우연일 수 있지만, 3~5 사이클에 같은 형태로 반복되면 그건 도구로 만들 가치가 있는 신호입니다. `/self-improve`가 누적된 약한 신호들을 한꺼번에 분석합니다.

## 약한 신호란 무엇인가

`/self-improve`가 1차로 분석하는 입력은 **모든 `artifacts/*/learnings.md`의 `applied: not-yet` 항목**입니다. 즉시 승격된 항목(`applied: rule`)은 이미 처리되었고, 일회성으로 판정된 항목(`applied: discarded`)은 의도적으로 제외됩니다.

`not-yet` 외에도 다음은 약한 신호로 간주됩니다 -- 원리는 **"사람 손이 한 번 더 닿은 모든 흔적"**입니다.

| 신호 | 의미 |
|---|---|
| 복구 경로를 거친 실패 | 빌드/테스트 실패 → 우회 → 재시도. 우회 자체가 학습 대상 |
| 같은 에러에 두 번 이상 걸림 | 한 번이면 우연, 두 번이면 패턴 |
| Human Review에서 반복 지적된 패턴 | 사용자가 같은 형태의 코드를 매번 고친다면 규칙 후보 |
| 수동 개입이 필요했던 상황 | AI가 끝내지 못해 사용자가 손댄 부분 |

이 신호들이 누적되면 한 사이클로는 보이지 않던 패턴이 보이기 시작합니다.

## 4가지 승격 메커니즘 — Rule / Hook / Skill / CLAUDE.md

같은 반복이라도 어떤 메커니즘으로 승격하는지가 비용 차이를 만듭니다. 기준은 **무엇이 가장 적은 비용으로 재발을 막는가**입니다.

| 반복 유형 | 승격 대상 | 예시 |
|---|---|---|
| 같은 제약 위반이 반복됨 | **Rule** | `.tsx에서 px 단위 금지`, `components/ui/* 직접 수정 금지` |
| 100% 기계적으로 잡아야 하는 것 | **Hook** | 커밋 전 `bun run build`, 저장 후 linter 자동 실행 |
| 잘못된 사용 패턴 — 올바른 방법을 가르쳐야 함 | **Skill** | shadcn 컴포넌트 설치·조합법, 특정 라이브러리 도입 가이드 |
| 아키텍처 결정 변경 | **CLAUDE.md** | 순방향 의존성 순서, 패키지 매니저 결정 |

새 Skill은 라이브러리·도구 단위로 만듭니다 -- 더 작게 쪼개면 Skill이 누적되어 검색·관리 비용이 커지고, 더 크게 묶으면 description으로 트리거하기 어려워집니다.

## 시뮬레이션 — 가상의 누적 learnings에서 무엇이 도출되는가

본 챕터에서는 feedme.wiki 한 사이클만 돌렸지만, 만약 4 사이클이 누적된 상태라면 `/self-improve`가 어떤 패턴을 발견할까요. 가상의 누적 learnings:

```
artifacts/feedme/learnings.md:
- "변환 결과 영역에 px 단위로 hardcoded margin 발견 → spacing token으로 수정"

artifacts/share-link/learnings.md:
- "복사 버튼에 px hardcoded → spacing token으로 수정"

artifacts/comments/learnings.md:
- "댓글 카드 padding이 px → spacing token으로 수정"

artifacts/profile/learnings.md:
- "프로필 헤더 margin이 px → spacing token으로 수정"
```

`/self-improve`가 분석한 결과:

```
패턴 — .tsx에서 px 단위가 4개 feature에 걸쳐 반복 발견됨
승격 대상 — Rule (제약 위반의 반복)
제안 — .claude/rules/no-px-in-tsx.md 생성

  description: tsx/jsx에서 px 단위 사용 금지
  globs: **/*.tsx, **/*.jsx
  내용: spacing token (gap-2, p-4 등) 또는 CSS variable을 사용한다.

→ 사용자 승인 후 적용. 이후 사이클부터 AI가 자동으로 이 규칙을 따른다.
```

비슷한 패턴이 100% 기계적으로 잡힐 수 있다면 Hook(예: lint 규칙 추가), 잘못된 사용법이라면 Skill, 아키텍처 결정이라면 CLAUDE.md로 갈 것입니다. 사용자가 어느 메커니즘으로 승격할지 최종 결정합니다.

## 한 사이클로는 보이지 않는다

본 챕터에서 만든 learnings.md 한 개로는 `/self-improve`의 가치가 잘 보이지 않습니다. 신호가 한 사이클에 한 번만 나타나면 패턴인지 우연인지 구분이 어렵습니다.

**Compound는 다음 feature 사이클부터 가치가 드러납니다.** 같은 프로젝트에서 두 번째, 세 번째 feature를 SDD 사이클로 만들고 나면, 첫 사이클에서 못 보던 반복이 보이기 시작합니다. 그때 `/self-improve`를 호출하면, 누적된 약한 신호가 한꺼번에 도구로 승격됩니다.

**도구가 갱신되면 다음 사이클은 더 빠릅니다.** Rule이 추가되어 같은 에러를 미리 막고, Skill이 추가되어 같은 라이브러리 사용법을 다시 가르치지 않아도 됩니다. **사이클이 도구를 만들고, 도구가 다음 사이클을 더 빠르게 만드는 -- 이것이 Compound Engineering의 정신입니다.**

## 핵심 포인트 정리

1. **두 종류의 학습**: 강한 신호는 /execute-plan Step 6에서 즉시 승격되고, 약한 신호는 /self-improve에서 누적된 뒤 승격됩니다
2. **약한 신호 = 사람 손이 닿은 흔적**: 우회·재시도·반복 지적·수동 개입이 모두 약한 신호. 한 번이면 우연, 여러 번이면 패턴
3. **4가지 승격 메커니즘**: 제약 위반 → Rule, 기계적 검사 → Hook, 잘못된 사용법 → Skill, 아키텍처 결정 → CLAUDE.md
4. **누적이 핵심**: 한 사이클로는 보이지 않습니다. 3~5 사이클이 쌓여야 약한 신호가 패턴으로 보이고, 그때 도구로 승격됩니다

## FAQ

- **Q: 한 사이클만 돈 지금, /self-improve를 호출하면 어떻게 되나요?**
  - A: 한 항목으로는 패턴이 약해서 의미 있는 제안이 나오지 않습니다. 두세 번째 사이클을 더 돈 뒤 호출하는 것이 효과적입니다.

- **Q: applied: discarded는 무엇인가요?**
  - A: 일회성으로 판정된 항목입니다. 패턴이 아니라 한 번의 우연으로 결론 났을 때 표시합니다. /self-improve의 분석 대상에서 제외됩니다.

- **Q: 같은 패턴을 Rule로 할지 Hook으로 할지 애매하면?**
  - A: AI가 판단하기 어려운 미묘한 위반(맥락에 따라 허용 여부가 달라짐)은 Rule, 100% 기계적으로 패턴 매칭으로 잡히는 것은 Hook입니다. 의심되면 Hook을 먼저 시도하고, 거짓 양성이 많으면 Rule로 내립니다.

## 이어서 배울 내용

SDD 6단계 사이클을 처음부터 끝까지 봤습니다. 다음 챕터에서는 한 Claude의 한계를 넘어, 여러 Agent가 동시에 협업하는 Agent Teams를 배웁니다.
